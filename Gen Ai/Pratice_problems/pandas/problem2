import pandas as pd

data = {
    "Product": ["Pen", "Book", "Bag", "Bottle", "Scale", "Pencil"],
    "Price": [20, 50, 500, 100, 30, 10]
}

df = pd.DataFrame(data)

print(df.head(3))
print(df.tail(3))